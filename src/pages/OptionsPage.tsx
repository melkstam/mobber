import React, { ReactElement } from 'react';

import { TimerState, TimerSend } from '../lib/timerMachine/timerMachine';
interface OptionsPageProps {

}

export default function OptionsPage({}: OptionsPageProps): ReactElement {
  return (
    <div>
      Options
    </div>
  );
}
